// Package checker provides a lightweight, extensible framework for scheduling
// and executing periodic health checks. It manages the execution lifecycle of
// various checks and dispatches notifications when the state of a check changes.
//
// The package supports configuration via JSON and allows for custom checks
// and notifiers to be registered.
package checker
